# Knowledge Base MCP Server Configuration
# Copy this file to .env and customize the values

# Directory containing your markdown notes
KB_DIR=/path/to/your/notes

# SQLite database file path for the search index
KB_DB=/path/to/database/.kb_index.db

# Optional: Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Transport mode: 'stdio' for local use, 'sse' for HTTP access
MCP_TRANSPORT=stdio

# Port for HTTP/SSE mode (only used when MCP_TRANSPORT=sse)
PORT=3399

# Host for HTTP/SSE mode (only used when MCP_TRANSPORT=sse)
HOST=0.0.0.0

# GitHub Personal Access Token for automatic git push operations (optional)
# If your notes directory is a git repository, the server can automatically
# commit and push changes when notes are created/updated/appended.
# Create a token at: https://github.com/settings/tokens
# Required scopes: repo (Full control of private repositories)
# Leave empty to disable automatic git sync
GIT_TOKEN=
